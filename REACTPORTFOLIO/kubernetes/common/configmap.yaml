apiVersion: v1
kind: ConfigMap
metadata:
  name: portfolio-config
  namespace: portfolio
  labels:
    app: portfolio
data:
  PORT: "5000"
  USE_MEMORY_DB: "false"
  NODE_ENV: "production"
  MONGO_HOST: "mongo-service"
  MONGO_PORT: "27017"
  MONGO_DB: "portfolio"
